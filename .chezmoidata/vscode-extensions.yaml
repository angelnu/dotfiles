# VS Code extensions common to every user, on top of each user's own
# overlay in vscode-extensions-<username>.yaml. See
# .chezmoitemplates/resolved-vscode-extensions for how the two combine,
# and .chezmoiscripts/run_onchange_after_30-vscode-extensions.sh.tmpl for
# how this actually gets installed (only on machines with the "develop"
# role, same as the visual-studio-code cask itself in packages.yaml).
#
# Empty for now - nothing identified yet as universally wanted rather
# than personal to one user's workflow; add here once something is.
vscode_extensions: []
