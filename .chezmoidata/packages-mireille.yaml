# Packages that apply only to mireille, on top of the common lists in
# packages.yaml. Same shape (category -> role -> os), see that file.
# Empty placeholder - fill in as needed, e.g.:
#   brews:
#     default:
#       any-os: [mongosh]
per_user_mireille: {}
